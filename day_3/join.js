/*
join : does not change original array
    returns a new string by concatenating all the elements in an array
    treats empty slots as undefined and add extra seprator 
*/

const arr = ['a', 's', 'd', 'f']
const join = arr.join('')
console.log(join);

console.log(arr);