/*
toString() 
    the string object overrides the toSting method of object
    returns the string itself 
*/

const x = new String("Hello world");
console.log(x); // without toString() returns an object
console.log(x.toString());